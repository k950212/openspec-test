## 1. Storefront Data And State

- [x] 1.1 Create a local product catalog data source with the fields needed for listing and pricing products.
- [x] 1.2 Add a Pinia cart store that supports adding items, changing quantities, removing items, and calculating subtotal values.
- [x] 1.3 Persist cart contents in localStorage so the shopping cart survives page refreshes.

## 2. Storefront And Navigation

- [x] 2.1 Update routing so the app includes storefront, cart, and checkout pages with clear navigation between them.
- [x] 2.2 Replace the current demo-oriented home experience with a product listing page that renders the catalog and add-to-cart actions.

## 3. Cart Experience

- [x] 3.1 Build a cart page that lists selected items, shows per-item quantity controls, and supports item removal.
- [x] 3.2 Display order totals on the cart page and provide a clear path from cart to checkout.

## 4. Checkout Flow

- [x] 4.1 Build a checkout page that shows the current order summary and collects basic customer information.
- [x] 4.2 Prevent checkout submission when the cart is empty and provide a path back to continue shopping.
- [x] 4.3 Show a simple order confirmation state after successful checkout submission and finalize the cart state appropriately.

## 5. Styling And Verification

- [x] 5.1 Update the app shell and page styling so the ecommerce flow feels cohesive across storefront, cart, and checkout.
- [x] 5.2 Verify the product list, cart updates, and checkout flow through project checks and manual validation.
- [x] 5.3 Add a consistent press animation for button interactions across storefront, cart, checkout, and navigation controls.
